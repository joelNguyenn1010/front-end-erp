import React from 'react'
import { Button } from 'antd'

const SubmitButtonComponent = () => {

    const onClick = () => {

    }

    return (
        
        <Button style={{width: '100%', marginBottom: '10px',}} type='primary' onClick={onClick}>Submit</Button>
    )
}
export default SubmitButtonComponent
